apiVersion: v1
kind: Service
metadata:
  name: {{ include "ws_server.fullname" . }}
  labels:
    {{- include "ws_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ws_server.selectorLabels" . | nindent 4 }}
