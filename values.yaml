worker:
  replicaCount: 1

global:
  conf:
    app:
      auth__username: "admin"
      auth__email: "admin@example.com"
    redis:
      url: "redis://redis:6379/0"
    ingress:
      enabled: false
      host: papermerge.minikube
    storage:
      enabled: false

