worker:
  replicaCount: 2

global:
  conf:
    app:
      django_settings_module: config.settings
      superuser:
        username: "admin"
        email: "admin@example.com"
    ingress:
      enabled: true
      host: papermerge.minikube
    db:
      type: "postgres"
      user: "postgres"
      name: "postgres"
    es:
      enabled: false
