worker:
  replicaCount: 3

global:
  conf:
    app:
      auth__username: "admin"
      auth__email: "admin@example.com"
      media_dir: /core_app/media
    redis:
      url: "redis://redis:6379/0"
    ingress:
      enabled: true
      host: paper.k8s.trusel.net
      cert:
        secretName: k8s-trusel-net-production-tls
    storage:
      enabled: true


autoscaling:
  enabled: false

replicaCount: 3

