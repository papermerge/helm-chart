apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "papermerge.fullname" . }}-test-connection"
  labels:
    {{- include "papermerge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "papermerge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
